package task2.computers;

import task2.components.CPU;
import task2.components.HDD;
import task2.components.RAM;

public class NetTop extends DesktopComputer {
    public NetTop(CPU cpu, HDD hdd, RAM ram) {
        super(cpu, hdd, ram);
    }
}
